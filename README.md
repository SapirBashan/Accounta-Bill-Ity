# Accounta-Bill-Ity
#this is a check

## Shared household members

Apply all SQL migrations in `supabase/migrations` in the Supabase SQL editor. The latest migration adds per-user categories and consolidates income into `משכורת` and `הכנסות נוספות`. Each account has private transactions, budgets, and category choices.